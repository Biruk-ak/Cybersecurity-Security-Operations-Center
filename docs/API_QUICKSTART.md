# Public API Quickstart

```bash
export SOC_TOKEN=...
curl -sS -H "Authorization: Bearer $SOC_TOKEN" \
  https://api.soc.example.com/v1/siem?status=open
curl -sS -H "Authorization: Bearer $SOC_TOKEN" \
  https://api.soc.example.com/v1/incident-response
```
