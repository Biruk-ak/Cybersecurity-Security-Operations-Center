async function request<T>(method: string, path: string, body?: unknown): Promise<T> { const res=await fetch(path,{method,headers:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body),credentials:'include'}); if(!res.ok) throw new Error(String(res.status)); return res.json(); }
export const apiClient={ get:<T>(p:string)=>request<T>('GET',p), post:<T>(p:string,b?:unknown)=>request<T>('POST',p,b), put:<T>(p:string,b?:unknown)=>request<T>('PUT',p,b), delete:<T>(p:string)=>request<T>('DELETE',p), getBlob:async(p:string)=>{const r=await fetch(p,{credentials:'include'}); if(!r.ok) throw new Error('blob'); return r.blob();} };
