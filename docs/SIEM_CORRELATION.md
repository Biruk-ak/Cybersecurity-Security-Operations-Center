# SIEM Correlation Guidance

## Principles
1. Correlate on entity + time window before severity rollup
2. Suppress duplicates with deterministic fingerprints
3. Emit SOAR-ready alerts with MITRE technique tags
