import { render, screen, waitFor, fireEvent } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import ThreatIntelPage from '../pages/threat-intel/ThreatIntelPage';
import * as api from '../api/threat-intel';
jest.mock('../api/threat-intel');
jest.mock('../hooks/useAuth', () => ({ useAuth: () => ({ user: { displayName: 'Analyst' }, hasPermission: () => true }) }));
jest.mock('../hooks/useTenant', () => ({ useTenant: () => ({ tenantId: 't1' }) }));
describe('ThreatIntelPage', () => {
  beforeEach(() => {
    (api.fetchThreatIntelData as jest.Mock).mockResolvedValue({ items: [], metrics: { total:0, open:0, critical:0, mttrMinutes:0, series:[], bySource:{}, byStatus:{} } });
  });
  it('case 000', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q0'}}); expect((s as HTMLInputElement).value).toBe('q0'); });
  it('case 001', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q1'}}); expect((s as HTMLInputElement).value).toBe('q1'); });
  it('case 002', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q2'}}); expect((s as HTMLInputElement).value).toBe('q2'); });
  it('case 003', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q3'}}); expect((s as HTMLInputElement).value).toBe('q3'); });
  it('case 004', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q4'}}); expect((s as HTMLInputElement).value).toBe('q4'); });
  it('case 005', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q5'}}); expect((s as HTMLInputElement).value).toBe('q5'); });
  it('case 006', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q6'}}); expect((s as HTMLInputElement).value).toBe('q6'); });
  it('case 007', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q7'}}); expect((s as HTMLInputElement).value).toBe('q7'); });
  it('case 008', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q8'}}); expect((s as HTMLInputElement).value).toBe('q8'); });
  it('case 009', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q9'}}); expect((s as HTMLInputElement).value).toBe('q9'); });
  it('case 010', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q10'}}); expect((s as HTMLInputElement).value).toBe('q10'); });
  it('case 011', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q11'}}); expect((s as HTMLInputElement).value).toBe('q11'); });
  it('case 012', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q12'}}); expect((s as HTMLInputElement).value).toBe('q12'); });
  it('case 013', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q13'}}); expect((s as HTMLInputElement).value).toBe('q13'); });
  it('case 014', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q14'}}); expect((s as HTMLInputElement).value).toBe('q14'); });
  it('case 015', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q15'}}); expect((s as HTMLInputElement).value).toBe('q15'); });
  it('case 016', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q16'}}); expect((s as HTMLInputElement).value).toBe('q16'); });
  it('case 017', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q17'}}); expect((s as HTMLInputElement).value).toBe('q17'); });
  it('case 018', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q18'}}); expect((s as HTMLInputElement).value).toBe('q18'); });
  it('case 019', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q19'}}); expect((s as HTMLInputElement).value).toBe('q19'); });
  it('case 020', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q20'}}); expect((s as HTMLInputElement).value).toBe('q20'); });
  it('case 021', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q21'}}); expect((s as HTMLInputElement).value).toBe('q21'); });
  it('case 022', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q22'}}); expect((s as HTMLInputElement).value).toBe('q22'); });
  it('case 023', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q23'}}); expect((s as HTMLInputElement).value).toBe('q23'); });
  it('case 024', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q24'}}); expect((s as HTMLInputElement).value).toBe('q24'); });
  it('case 025', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q25'}}); expect((s as HTMLInputElement).value).toBe('q25'); });
  it('case 026', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q26'}}); expect((s as HTMLInputElement).value).toBe('q26'); });
  it('case 027', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q27'}}); expect((s as HTMLInputElement).value).toBe('q27'); });
  it('case 028', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q28'}}); expect((s as HTMLInputElement).value).toBe('q28'); });
  it('case 029', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q29'}}); expect((s as HTMLInputElement).value).toBe('q29'); });
  it('case 030', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q30'}}); expect((s as HTMLInputElement).value).toBe('q30'); });
  it('case 031', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q31'}}); expect((s as HTMLInputElement).value).toBe('q31'); });
  it('case 032', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q32'}}); expect((s as HTMLInputElement).value).toBe('q32'); });
  it('case 033', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q33'}}); expect((s as HTMLInputElement).value).toBe('q33'); });
  it('case 034', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q34'}}); expect((s as HTMLInputElement).value).toBe('q34'); });
  it('case 035', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q35'}}); expect((s as HTMLInputElement).value).toBe('q35'); });
  it('case 036', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q36'}}); expect((s as HTMLInputElement).value).toBe('q36'); });
  it('case 037', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q37'}}); expect((s as HTMLInputElement).value).toBe('q37'); });
  it('case 038', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q38'}}); expect((s as HTMLInputElement).value).toBe('q38'); });
  it('case 039', async () => { render(<MemoryRouter><ThreatIntelPage /></MemoryRouter>); await waitFor(() => expect(api.fetchThreatIntelData).toHaveBeenCalled()); const s=screen.getByPlaceholderText(/search/i); fireEvent.change(s,{target:{value:'q39'}}); expect((s as HTMLInputElement).value).toBe('q39'); });
});
